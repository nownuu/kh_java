package api.thread;

public class Test01 {
	public static void main(String[] args) {
		// SingleThread VS multiThread (cf:process)
		// = process 는 실행중이 프로그램을 의미한다.
		// = 즉, 프로그램을 실행하면 process가 생성된다고 볼 수 있다.
		// = Thread는 process에서 여러개의 작업을 동시에 실행하기 위해 생성하는 작업 수행 단위
		// = process가 회사면 Thread는 작업자와 같다.
		// process에서 무조건 1개의 thread가 존재하며, 필요하담녀 더 생성할 수 있다.
		// 싱글 스레드는 스레드가 한개인 경우를 말하며, 멀티 스레드는 추가로 스레드를 생성한 경우를 말한다.
//		자바를 실해하면 main이라는 이름의 스레드가 기본적으로 생성된다
//		스레드는 동시에 1개의 작업을 수행할 수 있다.
		
	}
}
